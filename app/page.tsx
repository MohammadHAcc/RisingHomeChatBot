import SplitLayout from '@/components/Layout/SplitLayout';

/**
 * Root page — renders the split-screen layout.
 * Server component; all interactivity lives in client child components.
 */
export default function Home() {
  return <SplitLayout />;
}
